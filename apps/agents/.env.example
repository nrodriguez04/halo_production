# The agents runtime holds no provider keys. Everything it spends goes
# through the api, which applies the cost-control decision tree, budgets
# and the ledger, and attributes it to the tenant named per call.
INTERNAL_API_BASE_URL=http://localhost:3001/api
INTERNAL_API_TOKEN=change-me-32-bytes-minimum
